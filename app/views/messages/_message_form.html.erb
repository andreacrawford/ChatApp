  <%= form_for([@room, @room.messages.build], remote: true) do |f| %>
    <p>
      <%= f.label :body, "Type Here" %><br>
      <%= f.text_area :body %>
    </p>
    <p>
      <%= f.submit "Submit" %>
    </p>
  <% end %>